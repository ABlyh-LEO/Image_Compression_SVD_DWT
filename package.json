{
    "name": "svd-image-compression-hw2",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build",
        "build:single": "tsc -b && vite build --mode singlefile",
        "preview": "vite preview"
    },
    "devDependencies": {
        "typescript": "^5.6.3",
        "vite": "^5.4.9",
        "vite-plugin-singlefile": "^2.0.3"
    }
}